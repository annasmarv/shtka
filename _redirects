# Cloudflare Pages Routes Configuration

# Redirect root URL to login.html (actual redirect, not rewrite)
# requireLogout() di login.html akan auto-redirect ke dashboard.html jika user sudah login
/  /login.html  301
